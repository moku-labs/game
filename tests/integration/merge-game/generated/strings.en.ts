// generated by @moku-labs/game/assets, do not edit
import type { I18n } from "@moku-labs/game";

/** One plain argument: an element stays an element, a list and a number go through Intl. */
function argument(value: unknown, intl: I18n.IntlKit): I18n.Part {
  if (Array.isArray(value)) {
    return { kind: "text", text: intl.list().format(value as readonly string[]) };
  }

  if (typeof value === "number") return { kind: "text", text: intl.number().format(value) };

  if (
    typeof value === "object" &&
    value !== null &&
    "type" in value &&
    "props" in value &&
    "children" in value
  ) {
    return { kind: "element", node: value as I18n.ElementNode };
  }

  return { kind: "text", text: String(value) };
}

export default {
  "board.full": () => [{ kind: "text", text: "Board is full" }],
  "board.item": p => [{ kind: "text", text: (String(p.item) === "twig" ? "Twig" : String(p.item) === "log" ? "Log" : String(p.item) === "plank" ? "Plank" : String(p.item) === "crate" ? "Crate" : "Wood") }],
  "board.level": (p, intl) => [{ kind: "text", text: "Level " }, argument(p.level, intl)],
  "board.sawmill": () => [{ kind: "text", text: "Sawmill" }],
  "energy.later": () => [{ kind: "text", text: "Later" }],
  "energy.refill": (p, intl) => [{ kind: "text", text: "Refills in " }, argument(p.time, intl)],
  "energy.title": () => [{ kind: "text", text: "Out of energy" }],
  "energy.watch": () => [{ kind: "text", text: "Watch & refill" }],
  "gift.claim": () => [{ kind: "text", text: "Claim" }],
  "gift.coins": (p, intl) => [{ kind: "text", text: (intl.plural().select((p.coins as number)) === "one" ? "coin" : "coins") }],
  "gift.title": () => [{ kind: "text", text: "Daily gift" }],
  "home.play": () => [{ kind: "text", text: "Play" }],
  "hud.deliver": () => [{ kind: "text", text: "Deliver" }],
  "hud.need": (p, intl) => [argument(p.chain, intl), { kind: "text", text: " " }, argument(p.level, intl)],
  "hud.order": () => [{ kind: "text", text: "Order" }],
  "hud.settings": () => [{ kind: "text", text: "Settings" }],
  "orders.claim": () => [{ kind: "text", text: "Claim" }],
  "orders.deliver": () => [{ kind: "text", text: "Deliver" }],
  "orders.done": () => [{ kind: "text", text: "Order complete" }],
  "orders.reward": (p, intl) => [{ kind: "text", text: (intl.plural().select((p.coins as number)) === "one" ? `${intl.number().format((p.coins as number))} coin` : `${intl.number().format((p.coins as number))} coins`) }],
  "orders.stamp": () => [{ kind: "text", text: "Done!" }],
  "orders.title": (p, intl) => [{ kind: "text", text: "Order #" }, argument(p.number, intl)],
  "settings.bus": p => [{ kind: "text", text: (String(p.bus) === "music" ? "Music" : String(p.bus) === "sfx" ? "Effects" : "Volume") }],
  "settings.confirmBody": () => [{ kind: "text", text: "Board, coins and orders start over." }],
  "settings.confirmCancel": () => [{ kind: "text", text: "Cancel" }],
  "settings.confirmReset": () => [{ kind: "text", text: "Reset" }],
  "settings.confirmTitle": () => [{ kind: "text", text: "Reset everything?" }],
  "settings.english": () => [{ kind: "text", text: "English" }],
  "settings.percent": (p, intl) => [argument(p.percent, intl), { kind: "text", text: "%" }],
  "settings.reset": () => [{ kind: "text", text: "Reset progress" }],
  "settings.russian": () => [{ kind: "text", text: "Русский" }],
  "settings.tab": p => [{ kind: "text", text: (String(p.tab) === "audio" ? "Sound" : String(p.tab) === "language" ? "Language" : "More") }],
  "settings.title": () => [{ kind: "text", text: "Settings" }],
  "splash.loading": () => [{ kind: "text", text: "Loading…" }],
  "ui.gameName": () => [{ kind: "text", text: "Timber Town" }]
} satisfies I18n.CompiledMessages;
