// generated by @moku-labs/game/assets, do not edit
import type { I18n } from "@moku-labs/game";

/** One plain argument: an element stays an element, a list and a number go through Intl. */
function argument(value: unknown, intl: I18n.IntlKit): I18n.Part {
  if (Array.isArray(value)) {
    return { kind: "text", text: intl.list().format(value as readonly string[]) };
  }

  if (typeof value === "number") return { kind: "text", text: intl.number().format(value) };

  if (
    typeof value === "object" &&
    value !== null &&
    "type" in value &&
    "props" in value &&
    "children" in value
  ) {
    return { kind: "element", node: value as I18n.ElementNode };
  }

  return { kind: "text", text: String(value) };
}

export default {
  "hud.deliver": () => [{ kind: "text", text: "Отдать" }],
  "hud.need": (p, intl) => [argument(p.chain, intl), { kind: "text", text: " " }, argument(p.level, intl)],
  "hud.order": () => [{ kind: "text", text: "Заказ" }],
  "hud.settings": () => [{ kind: "text", text: "Настройки" }],
  "orders.claim": () => [{ kind: "text", text: "Забрать" }],
  "orders.done": () => [{ kind: "text", text: "Заказ выполнен" }],
  "orders.reward": (p, intl) => [{ kind: "text", text: (intl.plural().select((p.coins as number)) === "one" ? `${intl.number().format((p.coins as number))} монета` : intl.plural().select((p.coins as number)) === "few" ? `${intl.number().format((p.coins as number))} монеты` : `${intl.number().format((p.coins as number))} монет`) }],
  "settings.close": () => [{ kind: "text", text: "Закрыть" }],
  "settings.english": () => [{ kind: "text", text: "English" }],
  "settings.louder": () => [{ kind: "text", text: "Громче" }],
  "settings.quieter": () => [{ kind: "text", text: "Тише" }],
  "settings.tab": p => [{ kind: "text", text: (String(p.tab) === "audio" ? "Звук" : String(p.tab) === "language" ? "Язык" : "Ещё") }],
  "settings.title": () => [{ kind: "text", text: "Настройки" }],
  "settings.volume": (p, intl) => [{ kind: "text", text: `${(String(p.bus) === "music" ? "Музыка" : String(p.bus) === "sfx" ? "Звуки" : "Громкость")}: ` }, argument(p.percent, intl)]
} satisfies I18n.CompiledMessages;
